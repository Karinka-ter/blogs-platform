export type Blog = {
    "name": string,
    "description": string,
    "websiteUrl": string
}

export type BlogViewModel = {
    id: string
    "name": string,
    "description": string,
    "websiteUrl": string
}